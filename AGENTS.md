# AGENTS.md

## プロジェクト概要

技術スタック

- バックエンド: Python Fast API
- フロントエンド: JavaScript React
- データベース: SQLite

実装前に既存コードを確認し、既存の設計・命名規則を優先すること。

## ドキュメント

機能を実装・変更する前に、関連する以下のドキュメントを確認すること。

- 要件定義: `docs/requirements.md`

## 自律的な作業

通常の開発作業については、ユーザーへの確認なしで進めてよい。

以下は確認不要。

- リポジトリ内のファイルの作成・編集
- テストの実行
- バックエンドの起動
- フロントエンドの起動
- 実装によって発生したエラーの修正
- 関連ドキュメントの更新

軽微な実装判断については質問せず、既存コード・要件・各技術スタックの慣習から
最も自然な方法を選択して作業を継続すること。

## 判断に迷った場合

以下の優先順位で判断する。

1. ユーザーからの明示的な指示
2. `docs/requirements.md`
3. その他のプロジェクトドキュメント
4. 既存テスト
5. 既存コード
6. 買う技術スタックの一般的な慣習
7. 最も単純で影響範囲の小さい実装

以下に該当する場合のみ作業を停止して確認すること。

- データ損失の可能性がある
- セキュリティ上の重大な判断が必要
- 大規模なアーキテクチャ変更が必要
- 既存仕様を破壊する可能性がある
- 要件だけではユーザーの意図を合理的に判断できない

## テスト

コード変更後は関連するテストを実行すること。

テストが失敗した場合、すぐに作業を停止せず、

1. エラーを確認
2. 原因を調査
3. 修正
4. 再度テスト

を行うこと。

自分の変更が原因のエラーについては、可能な限り解消してから作業を完了すること。

## Git

以下は禁止。

- `git reset --hard`
- force push
- ユーザーの未コミット変更の破棄
- 関係ないファイルの変更
- secretsのcommit

明示的に指示されない限りcommitは行わないこと。
